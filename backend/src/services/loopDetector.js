// Basic loop detection placeholder: should scan transactions within window for mirrored amounts.
export function detectLoops(transactions = []) {
  // TODO: implement real matching logic.
  return transactions.filter(() => false);
}
